[package]
name = "idr-production-api-compile-fail"
version = "0.0.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[workspace]

[dependencies]
idr-runtime = { path = "../../crates/idr-runtime", default-features = false, features = ["production"] }
uuid = "1"
